/**
 * This hook provides access to the current color scheme of the device (light or dark).
 * It is a re-export of the `useColorScheme` hook from `react-native`.
 */
export { useColorScheme } from 'react-native';
